# 애플워치 운동 자동 전송 — iPhone 단축어 설정 가이드

2026-07-09부터 Garmin 워치 → Apple Watch로 전환. Apple Health는 서버 API가
없으므로 iPhone 단축어가 매일 20:55에 오늘 운동을 RunLog로 전송하고,
21:00 daily-sync가 (Garmin 활동과 합쳐) 기록 생성 + Instagram 게시에 사용합니다.

단축어는 **운동 1건마다 요청 1번**을 보내는 단순 구조입니다. 서버가 시작
시각 기준으로 병합하므로 같은 운동을 여러 번 보내도 중복되지 않습니다.

## 1. 단축어 만들기 (약 5분)

단축어 앱 → `+` → 이름: **RunLog 운동 전송**

검색창에서 아래 동작들을 차례로 추가:

**① "건강 샘플" 검색 → 「건강 샘플 찾기」** (또는 「모든 건강 샘플 찾기」)
- 샘플 유형: **운동** (Workouts)
- 필터 추가: `시작 날짜` → `이(가) 오늘임`

**② "반복" 검색 → 「각 항목 반복」**
- 반복 대상: `건강 샘플` (①의 결과 — 자동으로 들어감)

**③ 반복 블록 *안*에 "URL" 검색 → 「URL 콘텐츠 가져오기」**
- URL: `https://runlog.life/api/workouts/ingest`
- 「방법 표시」 펼치기 → 방법: **POST**
- 헤더 추가: 키 `Authorization`, 값 `Bearer <CRON_SECRET>`
  - CRON_SECRET 값은 Mac 터미널에서 복사(클립보드로):
    `cd runlog폴더 && grep '^CRON_SECRET=' .env.local | cut -d'"' -f2 | pbcopy`
  - `Bearer` 한 칸 띄고 붙여넣기
- 요청 본문: **JSON** → 필드 4개 추가 (모두 유형 «텍스트»):

  | 키 | 값 (변수 선택 → `반복 항목` 탭 → 속성 선택) |
  |---|---|
  | `start` | 반복 항목의 **시작 날짜** |
  | `type` | 반복 항목의 **운동 유형** |
  | `durationMinutes` | 반복 항목의 **지속 시간** |
  | `distanceKm` | 반복 항목의 **거리** (단위 선택 가능하면 **km**) |
  | `calories` | 반복 항목의 **활성 에너지** |

  값 칸을 탭 → 「변수 선택」 → `반복 항목`을 고른 뒤 항목을 한 번 더 탭하면
  속성(시작 날짜/운동 유형/…)을 고를 수 있습니다.
  단위가 붙은 텍스트("10.52 km", "58:24", "1시간 5분")여도 서버가 알아서
  숫자로 변환합니다.

**④ (선택) 반복 블록 *밖*에 「알림 표시」** — 내용: `URL 콘텐츠`
- 마지막 응답의 `"ok": true, "saved": {...}`로 성공 확인용

## 2. 7/9~17 밀린 운동 백필 (최초 1회)

1. ①의 필터를 잠시 `시작 날짜` → **`이(가) 지난 30일 이내임`** 으로 변경
2. 단축어 이름을 눌러 **수동 실행** (건강 데이터 접근 허용 팝업 → 허용)
3. 완료되면 Claude에게 "운동 데이터 보냈어요"라고 알리기 → 기록 생성 진행
4. 필터를 다시 `오늘`로 되돌리기

## 3. 매일 20:55 자동 실행

자동화 탭 → `+` → 개인 자동화 → **특정 시간**: 매일 오후 8:55
→ 단축어: **RunLog 운동 전송** → **즉시 실행** (확인 없이)

## 참고

- 같은 운동을 다시 보내면 시작 시각 기준으로 대체됩니다 (중복 기록 없음)
- Garmin 활동이 있는 날은 두 소스가 합쳐집니다 (7/8 이전 Garmin, 7/9 이후 Apple)
- 사진 단축어(20:50)와 별개입니다 — 둘 다 설정하세요 ([PHOTO_AUTO_SELECT_GUIDE.md](PHOTO_AUTO_SELECT_GUIDE.md))
